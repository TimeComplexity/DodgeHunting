using UnityEngine;
using System.Collections;

public class Fairy : MonoBehaviour {

	/*public GameObject _player;
	private int count=0;
	private Fire com;
	private int resetCount;
	// Use this for initialization
	void Start () {
		_player = GameObject.Find ("/Player");
		com = _player.GetComponent<Fire> ();
		resetCount = com.resetCount;
	}

	void OnCollisionEnter(Collision coll)
	{
		if(coll.gameObject.name=="Player")
		{
			Player_Move pm = coll.gameObject.GetComponent<Player_Move>();
			pm.maxSpeed+=pm._maxSpeed*0.05f;
			++com.fairyPlayer;
			Destroy(gameObject);
		}
	}

	// Update is called once per frame
	void Update () {
		if(Time.timeScale==0)
		{
			return;
		}
		++count;
		if(count==600/(resetCount+1))
		{
			Destroy (gameObject);
		}
	}*/
}
