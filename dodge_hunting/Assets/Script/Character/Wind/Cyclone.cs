using UnityEngine;
using System.Collections;

public class Cyclone : MonoBehaviour {

/*	public GameObject _player;
	private int count = 0;
	private Wind com;
	// Use this for initialization
	
	void Start () {
		_player = GameObject.Find ("/Player");
		com = _player.GetComponent<Wind> ();
	}

	
	// Update is called once per frame
	void Update () {
		if(Time.timeScale==0)
			return;
		if (count < 13) 
			transform.Translate (0.0f, 0.4f, 0.0f);
		++count;
		if(count==13)
			tag="Cyclone";
		else if(count>=600*com.windSkill)
			Destroy(gameObject);
	}*/
}
